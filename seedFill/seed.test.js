const expect = require('chai').expect;

describe('The seed algorithm', () => {
	it('takes the 2D array of points and a starting point and returns a list of points to paint', () => {
		const testArray = 
	});
});